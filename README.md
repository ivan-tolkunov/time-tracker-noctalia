# Time Tracker Plugin Scaffold

Minimal greenfield scaffold for the Noctalia time-tracker plugin.

## Local setup

1. Install dependencies: `npm install`
2. Run tests: `npm test`
3. Run TypeScript checks: `npm run typecheck`

This milestone intentionally includes only placeholder QML entrypoints and a TypeScript test harness.
